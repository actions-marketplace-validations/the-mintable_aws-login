# `aws-login` GitHub Action

A GitHub Action to login to AWS.

Use this:

```yaml
    - name: Login to AWS
      uses: the-mintable/aws-login@v1
      with:
        aws-account: 123
        aws-region: xyx
```
